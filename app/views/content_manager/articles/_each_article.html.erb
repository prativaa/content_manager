<td><%= article.id %></td>
<td><%= article.title %></td>
<td><%= article.published_at ? article.published_at.strftime("%y年%m月%d日") : "Unpublished" %></td>
<td><%= article.updated_at.strftime("%y年%m月%d日") %></td>
<td>
  <a class="btn btn-default" href="articles/<%= article.id %>/edit">
    <span class="glyphicon glyphicon-pencil"></span>
  </a>
</td>
<td>
  <%= link_to article_path(article), class: "btn btn-default",  method: :delete, data: { confirm: "#{article.title} Are you sure？" } do %>
    <span class="glyphicon glyphicon-remove"></span>
  <% end %>
</td>
<td><%= article.status %></td>